name: Build and Deploy to Kubernetes

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.vars.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - id: vars
        run: echo "image_tag=${GITHUB_SHA}" >> $GITHUB_OUTPUT

      - name: Build Docker images
        run: |
          docker build -t ${{ secrets.REGISTRY_URL }}/ai-service:${{ steps.vars.outputs.image_tag }} ./ai-service
          docker build -t ${{ secrets.REGISTRY_URL }}/agent-ui:${{ steps.vars.outputs.image_tag }} ./agent-ui

      - name: Push Docker images
        run: |
          echo ${{ secrets.REGISTRY_PASSWORD }} | docker login ${{ secrets.REGISTRY_URL }} \
            --username ${{ secrets.REGISTRY_USERNAME }} --password-stdin
          docker push ${{ secrets.REGISTRY_URL }}/ai-service:${{ steps.vars.outputs.image_tag }}
          docker push ${{ secrets.REGISTRY_URL }}/agent-ui:${{ steps.vars.outputs.image_tag }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      # Cloud auth
      - name: Cloud auth (Azure)
        uses: ./.github/workflows/auth-azure.yml@main

      # Context setup
      - name: Context (Azure)
        uses: ./.github/workflows/context-azure.yml@main

      - name: Create/Update Kubernetes Secrets
        run: |
          kubectl create secret generic ai-service-secrets \
            --from-literal=OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }} \
            --from-literal=OPENAI_PROJECT_ID=${{ secrets.OPENAI_PROJECT_ID }} \
            --from-literal=GOOGLE_SEARCH_API_KEY=${{ secrets.GOOGLE_SEARCH_API_KEY }} \
            --from-literal=GOOGLE_SEARCH_CSE_ID=${{ secrets.GOOGLE_SEARCH_CSE_ID }} \
            --dry-run=client -o yaml | kubectl apply -f -

      # Deployment of docker images to k8s cluster
      - name: Deploy Milvus (Helm)
        run: |
          helm repo add milvus https://milvus-io.github.io/milvus-helm/
          helm repo update
          helm upgrade --install milvus milvus/milvus \
            --namespace mychatbot --create-namespace \
            -f k8s/milvus-azure-values.yaml

      - name: Deploy ai-service
        run: |
          helm upgrade --install ai-service ./helm/ai-service \
            --namespace mychatbot \
            --set registry=${{ secrets.REGISTRY_URL }} \
            --set tag=${{ needs.build.outputs.image_tag }}


      - name: Deploy agent-ui
        run: |
          helm upgrade --install agent-ui ./helm/agent-ui \
            --namespace mychatbot \
            --set registry=${{ secrets.REGISTRY_URL }} \
            --set tag=${{ needs.build.outputs.image_tag }}
